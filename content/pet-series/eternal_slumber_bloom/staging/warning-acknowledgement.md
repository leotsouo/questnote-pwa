# 永眠花海正式發布 Warnings 承認紀錄

發布時間：正式 Publish 前確認

## 正式接受（不阻擋發布）

### PET_NO_STANDARD ×12
永眠花海為限定系列，不加入 standard。

### LORE_PERSONALITY_FEW ×12
QuestNote 本系列正式 personality 規格為每隻兩個性格詞，資料內容完整，不為了消除 Warning 擅自增加第三個性格。

### PROMPT_MISSING ×12
圖片已定稿且具有來源備份。圖片 Prompt 不屬於 pets.json 或 pets-lore.json 的正式執行欄位。本版本不因補 Prompt 而延後發布。

### IMAGE_LARGE ×12
部分圖片經無損 PNG 最佳化後仍落在 2～5 MB。保留原尺寸 1254×1254，不裁切、不縮放、不轉格式。等待 iPhone 實機測試判斷是否需要後續效能版本。

## 預期發布後消失

### PET_POOLTAG_UNUSED ×12
新增並啟用正式活動池 `eternal_slumber_bloom` 後應消失。

### PET_NO_ACTIVE_POOL ×12
同上。

若發布後仍出現上述兩類，視為錯誤。
